import { FC } from 'react';

const UnauthoriseMessage: FC = () => {
  return (
    <p className="text-lg text-gray-500">
      Будь ласка, увійдіть, щоб переглянути свої тренування.
    </p>
  );
};

export default UnauthoriseMessage;
